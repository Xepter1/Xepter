services:
  xepter:
    build: .
    image: xepter-portfolio-lab
    container_name: xepter-portfolio-lab
    restart: unless-stopped
    ports:
      # host:container — Lab läuft auf 8089 (Prod belegt 8088)
      - "8089:80"
